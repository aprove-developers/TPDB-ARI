; example from footnote 5 the IJCAR 16 paper "Lower Runtime Bounds for Integer Programs"
; it's terminating, but its runtime is unbounded

(format LCTRS)
(theory Ints)
(fun l0 (-> Int Int))
(fun l1 (-> Int Int))
(entrypoint l0)
(rule (l0 x) (l1 x'))
(rule (l1 x) (l1 (- x 1)) :guard (> x 0))

